#include <stdio.h>

#define ABS(x) ((x) < 0 ? -(x) : (x))

int main()
{
    int num;

    printf("Enter a number: ");
    scanf("%d", &num);

    printf("Absolute Value = %d", ABS(num));

    return 0;
}
Sample Output
Enter a number: -25
Absolute Value = 25
