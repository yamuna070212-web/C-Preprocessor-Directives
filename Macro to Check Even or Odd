#include <stdio.h>

#define EVEN(n) ((n)%2==0 ? "Even" : "Odd")

int main()
{
    int n;

    printf("Enter Number: ");
    scanf("%d",&n);

    printf("%s", EVEN(n));

    return 0;
}
Sample Output
Enter Number: 25

Odd
