#include <stdio.h>

#define SWAP(a,b) { int temp=a; a=b; b=temp; }

int main()
{
    int a = 10, b = 20;

    SWAP(a,b);

    printf("a = %d\n", a);
    printf("b = %d", b);

    return 0;
}
Sample Output
a = 20
b = 10
